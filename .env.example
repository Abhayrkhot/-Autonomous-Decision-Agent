# No API keys or environment variables are required by this baseline.
# This file is a template only; the application does not load dotenv files.
# Optional durable storage; export explicitly before running.
# DATABASE_URL=postgresql://postgres:local-test-only@localhost:55432/agent
